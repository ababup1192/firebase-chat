import {List, Map} from "immutable";
import {UserInfoUtil} from "../src/script/utils/userInfo";

describe("UserInfoUtil", () => {
    describe("toFirebaseUserInfo", () => {
        it("should return double list", () => {
            chai.assert.isTrue(true);
        });
    });
});
